steps:
  - template: login-registory.yaml
  - script: |
      docker pull $(registryName)/$(imageName):$(tag)
      docker run --rm $(registryName)/$(imageName):$(tag) flake8 ./utils.py
    displayName: 'Run Flake8'
    continueOnError: false