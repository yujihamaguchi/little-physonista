steps:
  - script: |
      cd $(PROJECT_ROOT_DIR)
      python -m twine upload --repository-url $(REPOSITORY_ADDRESS) --username "unused" --password $(System.AccessToken) dist/*
    displayName: 'Publish package'
    continueOnError: false
